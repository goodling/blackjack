.login {
    padding: rem(16);
    width: rem(500px);
    margin: 0 auto;
    margin-top: rem(200px);
    background: $felt-table-top;                                       // Color variables
    gameFont();

    .login__greeting {
        margin-bottom: 1em
    }
}